# Supabase local-only files — not committed
# This file is generated by `supabase init` to keep local secrets and
# runtime artifacts out of git.

# Local secrets (created on first `supabase start` with `supabase db push`)
.env

# Local Docker volumes and runtime cache
.branches/
.temp/
